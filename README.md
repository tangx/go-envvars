# envcfg library

## todo

+ env struct
  +[x] nested env: `sValue:=rv.Filed(i); sValue.Interface()`

+ env field
  - env
  - envDefault

+ env support type
  + time.Duration
  + int,int8,int16,int32,int64
  + string
  + bool

+ env action
  + getenv
  + setenv
  + `env` to `config.yml`

